export interface CountryRequestModel {
    countryName: string;
    countryCode: string;
}

export interface CountryResponseModel {
    countryId: number;
    countryName: string;
    countryCode: string
}